27) Create a program that calculates the average of different ages


*Input: Read the number of ages.

*Input: Read the ages and store them in an array.

*Calculate the sum of the ages:

   Use a loop to iterate through the array.

   Add each age to a running total (sum).

*Calculate the average:

   Divide the sum by the number of ages.

*Output: Display the average.